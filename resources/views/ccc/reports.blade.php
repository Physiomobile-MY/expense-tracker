@extends('layouts.app')

@section('content')
<div class="mb-5 flex flex-col gap-3 sm:flex-row sm:items-end sm:justify-between"><div><p class="text-sm font-semibold text-[#D71920]">Cashflow, debt, operations</p><h1 class="text-2xl font-bold text-gray-950">Reports</h1></div><a class="pm-btn-secondary" href="{{ request()->fullUrlWithQuery(['export' => 'csv']) }}">Export CSV</a></div>
<form class="pm-card mb-5 grid gap-3 p-4 md:grid-cols-3"><div><label class="pm-label">From</label><input class="pm-input" type="date" name="from" value="{{ $from->format('Y-m-d') }}"></div><div><label class="pm-label">To</label><input class="pm-input" type="date" name="to" value="{{ $to->format('Y-m-d') }}"></div><div class="flex items-end"><button class="pm-btn-primary w-full">Run Report</button></div></form>
<div class="grid gap-4 lg:grid-cols-3"><section class="pm-card p-4"><h2 class="font-bold text-gray-950">Cashflow Summary</h2><div class="mt-3 space-y-2 text-sm"><div class="flex justify-between"><span>Inflow</span><strong>RM {{ number_format((float) $cashflow->sum('total_inflow'), 2) }}</strong></div><div class="flex justify-between"><span>Outflow</span><strong>RM {{ number_format((float) $cashflow->sum('total_outflow'), 2) }}</strong></div><div class="flex justify-between"><span>Latest Closing</span><strong>RM {{ number_format((float) ($cashflow->last()?->closing_balance ?? 0), 2) }}</strong></div></div></section><section class="pm-card p-4"><h2 class="font-bold text-gray-950">Category Spending</h2><div class="mt-3 space-y-2 text-sm">@forelse($categorySpending as $row)<div class="flex justify-between"><span>{{ $row->category?->name ?: 'Uncategorised' }}</span><strong>RM {{ number_format((float) $row->total, 2) }}</strong></div>@empty<p class="text-gray-500">No spending.</p>@endforelse</div></section><section class="pm-card p-4"><h2 class="font-bold text-gray-950">Planned vs Actual</h2><div class="mt-3 space-y-2 text-sm"><div class="flex justify-between"><span>Planned</span><strong>RM {{ number_format((float) $plannedVsActual->sum('planned_amount'), 2) }}</strong></div><div class="flex justify-between"><span>Actual</span><strong>RM {{ number_format((float) $plannedVsActual->sum('actual_amount_paid'), 2) }}</strong></div></div></section></div>
<section class="pm-card mt-5 overflow-hidden"><div class="border-b border-gray-100 px-4 py-3"><h2 class="font-bold text-gray-950">Creditor Aging</h2></div><table class="w-full text-left text-sm"><thead class="bg-gray-50 text-xs uppercase text-gray-500"><tr><th class="px-4 py-3">Creditor</th><th>Invoice</th><th>Due</th><th>Status</th><th class="text-right">Outstanding</th></tr></thead><tbody class="divide-y divide-gray-100">@forelse($aging as $debt)<tr><td class="px-4 py-3 font-semibold">{{ $debt->creditor->creditor_name }}</td><td>{{ $debt->invoice_number }}</td><td>{{ $debt->due_date?->format('d M Y') }}</td><td>{{ str($debt->status)->headline() }}</td><td class="px-4 py-3 text-right font-bold">RM {{ number_format((float) $debt->outstanding_amount, 2) }}</td></tr>@empty<tr><td colspan="5" class="px-4 py-6 text-center text-gray-500">No outstanding debts.</td></tr>@endforelse</tbody></table></section>
@endsection

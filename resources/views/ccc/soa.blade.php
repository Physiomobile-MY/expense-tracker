@extends('layouts.app')

@section('content')
<div class="mb-5 flex flex-col gap-3 sm:flex-row sm:items-end sm:justify-between"><div><p class="text-sm font-semibold text-[#D71920]">Statement of Account</p><h1 class="text-2xl font-bold text-gray-950">SOA</h1></div><a class="pm-btn-secondary" href="{{ request()->fullUrlWithQuery(['export' => 'csv']) }}">Export CSV</a></div>
<form class="pm-card mb-5 grid gap-3 p-4 md:grid-cols-4"><div><label class="pm-label">Creditor</label><select class="pm-input" name="creditor_id">@foreach($creditors as $creditor)<option value="{{ $creditor->id }}" @selected($selectedCreditor?->id === $creditor->id)>{{ $creditor->creditor_name }}</option>@endforeach</select></div><div><label class="pm-label">From</label><input class="pm-input" type="date" name="from" value="{{ request('from') }}"></div><div><label class="pm-label">To</label><input class="pm-input" type="date" name="to" value="{{ request('to') }}"></div><div class="flex items-end"><button class="pm-btn-primary w-full">Filter</button></div></form>
<section class="pm-card overflow-hidden"><div class="border-b border-gray-100 px-4 py-3"><h2 class="font-bold text-gray-950">{{ $selectedCreditor?->creditor_name ?: 'No creditor selected' }}</h2></div><table class="w-full text-left text-sm"><thead class="bg-gray-50 text-xs uppercase text-gray-500"><tr><th class="px-4 py-3">Date</th><th>Reference</th><th>Description</th><th>Debit</th><th>Credit</th><th class="text-right">Running Balance</th></tr></thead><tbody class="divide-y divide-gray-100">@forelse($entries as $entry)<tr><td class="px-4 py-3">{{ $entry->date->format('d M Y') }}</td><td>{{ $entry->reference }}</td><td>{{ $entry->description }}</td><td>RM {{ number_format((float) $entry->debit, 2) }}</td><td>RM {{ number_format((float) $entry->credit, 2) }}</td><td class="px-4 py-3 text-right font-bold">RM {{ number_format((float) $entry->running_balance, 2) }}</td></tr>@empty<tr><td colspan="6" class="px-4 py-6 text-center text-gray-500">No SOA entries yet.</td></tr>@endforelse</tbody></table></section>
@endsection
